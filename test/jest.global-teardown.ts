// jest.global-teardown.ts

import { execSync } from 'child_process';

export default async () => {
  // Example: Clean up or stop services here
  // e.g., Drop test database or stop any running services

  // Additional teardown can be performed here
};
